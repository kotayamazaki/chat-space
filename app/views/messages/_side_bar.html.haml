.SideBar
  .LeftHeader
    .LeftHeader__box
      .UserName
        = current_user.name
      .Icon
        = link_to new_group_path, class: "Icon__btn" do
          = icon('far', 'edit',  class: "Icon__btn--vew")
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog',  class: "Icon__btn--vew")
  .GroupList
    - current_user.groups.each do |group|
      .Group
        = link_to '#' do
          .Group__name
            = group.name
          .Group__message
            メッセージはまだありません